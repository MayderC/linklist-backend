// lista hard code, de links no permitidos.

// todo collection, para almacenar links no permitidos 
// para administrarlos desde un panel de administraion

export const whitekList = () : string[]=>{
  
  const links : string[] =[]

  return links
}